"""Core domain modules for Obsidian Local REST API.

Modules:
    server   — GET / health check
    active   — Active file operations (/active/)
    vault    — Vault file and directory operations (/vault/)
    periodic — Periodic note operations (/periodic/)
    search   — Search operations (/search/)
    commands — Obsidian command listing and execution (/commands/)
    tags     — Tag listing (/tags/)
    ui       — Obsidian UI operations (/open/)
"""
